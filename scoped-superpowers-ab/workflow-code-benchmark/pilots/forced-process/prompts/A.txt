Implement the task in contract.md in this isolated workspace. Read the existing tags.py and implement only normalize_tags; write focused unittest tests and run them. Final response must match the supplied JSON schema. Use Python 3.10 standard library only. No network, external files, dependency installation, servers, delegation, Git operations, commit or push. Only tags.py, tests/ and docs/ may change. All specification choices in contract.md are fixed. You are authorized to proceed through the assigned process without asking questions or waiting for human approvals; if a workflow calls for an approval, record it as pre-authorized for this experiment. Do not add features. No worktree, external reviewer or visual companion. If review is required, review your own artifacts locally.

Both full workflow sources and a scoped router are available under workflows/. Use only the arm specified below. Human approval waits and multi-agent review are excluded equally for all arms. Final JSON: implemented (boolean), tests_passed (boolean), summary (at most 160 characters).

ARM A — Direct implementation. Do not load or apply workflow documents. Implement the function, add focused tests, run the tests, and return the final JSON. Do not create design or plan documents.
