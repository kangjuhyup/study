# Settlement contract
Audit settlement.py without editing it. Compute net settled cents per account for 2026-09-01 (inclusive) through 2026-09-08 (exclusive), by lexicographic ISO date. Count only status=settled. Deduplicate by event_id, keeping its first appearance in the entire input BEFORE filtering. A charge adds its positive amount_cents; a refund subtracts its positive amount_cents. Include alpha, beta, gamma even if zero. Find exactly the implementation defects that violate this contract. Name each defect with the uppercase violated concept followed by _RULE (e.g., STATUS_RULE for incorrect status filtering). Do not report hypothetical issues outside this contract.
Use these exact identifiers for violated concepts: DATE_RULE, REFUND_RULE, DEDUP_RULE, STATUS_RULE, ACCOUNT_RULE, CHARGE_RULE. Return only identifiers for actual defects.
