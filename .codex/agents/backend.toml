model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = "Use $rv-workflow:project-toolchain before executable work, $rv-workflow:backend for backend tasks, and $rv-workflow:task-progress only at tracked medium/large phase boundaries. Preserve project rules and accepted contracts; do not weaken tests."
